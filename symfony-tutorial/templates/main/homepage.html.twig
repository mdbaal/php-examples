{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Helloooo{% endblock %}

{% block body %}
    <h1 class="text-4xl">
        {{ message | lower }}
    </h1>

    <h2>Repository has {{ itemCount }} items</h2>

    {% for item in items %}
        {{ include('partials/_item.html.twig') }}
    {% endfor %}

{% endblock %}
