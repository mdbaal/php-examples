<div class="shadow mb-2 w-fit mx-auto p-2 rounded bg-neutral-50 text-2xl">
    <span><a href="{{ path('app_item_show',{ name: item.name}) }}">{{ item.name }}</a></span>
    <span>{{ item.price }}</span>
</div>
